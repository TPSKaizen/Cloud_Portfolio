trigger: none # No CI Triggers

resources:
  repositories:
  - repository: templates
    type: git
    name: templates

variables:
- group: general
- group: apiRelated

pool: $(selfhostedpool)

stages:
- template: build_api_stage.yaml@templates
  parameters:
      apiName: 'messages'
      environmentName: 'dev'
      sonarqubeProjectKey: $(messagesSQKey)
      sonarqubeProjectName: $(messagesSQName)
      sonarqubeConnectionName: $(sqserviceconnection)

- template: docker_build_scan_push_stage.yaml@templates
  parameters:
      apiName: 'messages'
      environmentName: 'dev'
      repositoryName: 'messages'
      acrName: $(acrName)
